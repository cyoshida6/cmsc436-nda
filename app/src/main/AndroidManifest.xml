<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cmsc436_nda">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cmsc436nda"
        tools:targetApi="31">


        <activity
            android:name=".CorsiBlockTappingActivity"
            android:exported="false"
            android:label="Corsi-Block Tapping"/>
        <!-- Login Activity -->
        <activity
            android:name=".LogInActivity"
            android:exported="true"
            android:label="Neurological Diagnostic Aid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"

            android:exported="true">
        </activity>
        <!--Trailmaking Activity-->
        <activity
            android:label="List of Mobile OS"
            android:name=".TrailMakingActivity"
            android:exported="true">
        </activity>
        <!--GoNoGo Activity-->

        <activity
            android:name=".com.example.cmsc436_nda.GoNoGoActivity"
            android:exported="true"
            android:label="Go No Go">

        </activity>
    </application>

</manifest>